import api from './api';const USE_MOCKS=import.meta.env.VITE_USE_MOCK_DATA==='true';
const providers={generatedAt:new Date().toISOString(),providers:[{id:'open-meteo',name:'Open-Meteo',category:'Weather nowcast',configured:true,mode:'LIVE_NO_KEY',capabilities:['hourly precipitation','rain probability','temperature','humidity','wind'],note:'Live public forecast API; no key required.'},{id:'google-maps',name:'Google Maps Platform',category:'Traffic-aware mobility',configured:false,mode:'READY_FOR_KEY',capabilities:['Routes API','traffic-aware ETA','route polyline','route comparison'],note:'Add GOOGLE_MAPS_API_KEY to enable live routing.'},{id:'sentinel-hub',name:'Sentinel Hub',category:'Satellite intelligence',configured:false,mode:'READY_FOR_OAUTH',capabilities:['Sentinel-2 catalog','scene quality filtering','water-index pipeline','imagery processing hook'],note:'Add Sentinel Hub OAuth credentials.'},{id:'postgis',name:'PostgreSQL + PostGIS',category:'Spatial data fabric',configured:true,mode:'INTERNAL',capabilities:['risk polygons','drain/sensor geometry','spatial joins','flood-aware routing'],note:'Primary operational geospatial store.'},{id:'aquaguard-ai',name:'AquaGuard AI Service',category:'AI / ML',configured:true,mode:'INTERNAL',capabilities:['XGBoost flood risk','YOLO drainage vision','multi-signal fusion'],note:'FastAPI AI service.'}]};
function forecast(){const v=[1.2,2.6,5.8,9.4,14.1,18.6,12.9,8.2,4.7,2.1,1,.4],n=new Date();return{provider:'OPEN_METEO_DEMO',summary:{horizonHours:12,totalPrecipitationMm:81,peakHourlyMm:18.6,peakTime:new Date(n.getTime()+5*3600000).toISOString(),wetHours:8,severity:'HIGH'},hourly:v.map((x,i)=>({time:new Date(n.getTime()+i*3600000).toISOString(),precipitationMm:x,probabilityPct:Math.min(98,35+i*6),temperatureC:25-i*.15,humidityPct:72+i*1.5,windKmh:8+i*.6}))}}
export async function getProviderStatus(){if(USE_MOCKS)return providers;return(await api.get('/providers/status')).data}export async function getWeatherNowcast(params={}){if(USE_MOCKS)return forecast();return(await api.get('/providers/weather/nowcast',{params})).data}export async function getSentinelScenes(params={}){if(USE_MOCKS)return{configured:false,provider:'SENTINEL_HUB',collection:'sentinel-2-l2a',message:'Demo mode: add OAuth credentials for live scene search.',scenes:[]};return(await api.get('/providers/satellite/scenes',{params})).data}export async function computeGoogleRoute(payload){if(USE_MOCKS)return{configured:false,provider:'GOOGLE_ROUTES',demo:{distanceMeters:6400,durationSeconds:1120,trafficDurationSeconds:1280,routeLabel:'Traffic-aware baseline route'}};return(await api.post('/providers/google/routes',payload)).data}
