import PageHeader from '../../../components/layout/PageHeader';import {useAuth} from '../../../context/AuthContext';import './Profile.css';export default function Profile(){const {user}=useAuth();return <div className="animate-in"><PageHeader eyebrow="Account" title="Profile" copy="Manage your RIVORA identity and notification preferences."/><section className="profile-card glass"><div className="profile-avatar">{(user?.name||'AR').split(' ').map(x=>x[0]).join('').slice(0,2)}</div><div><h3>{user?.name||'Citizen'}</h3><p>{user?.email||'Not available'}</p><span>{user?.role||'citizen'} account</span></div></section></div>}