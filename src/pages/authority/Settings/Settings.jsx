import PageHeader from '../../../components/layout/PageHeader';import './Settings.css';export default function Settings(){return <div className="animate-in"><PageHeader eyebrow="System configuration" title="Settings" copy="Frontend controls prepared for backend integration."/><div className="settings-grid"><section className="glass settings-card"><h3>API Integration</h3><label>API Base URL<input className="input" defaultValue={import.meta.env.VITE_API_BASE_URL||(import.meta.env.PROD?'/api':'http://localhost:5000/api')}/></label><label className="toggle"><input type="checkbox" defaultChecked={import.meta.env.VITE_USE_MOCKS!=='false'}/><span>Use mock data while APIs are unavailable</span></label></section><section className="glass settings-card"><h3>Real-time Events</h3><p className="muted">Socket.IO is intentionally not faked. The frontend is ready for future events:</p><div className="event-list">{['newReport','riskUpdated','newAlert','maintenanceAssigned','maintenanceUpdated','sensorUpdated'].map(x=><span key={x}>{x}</span>)}</div></section></div></div>}