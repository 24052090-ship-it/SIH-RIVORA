import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ShieldCheck,Eye,EyeOff,ArrowLeft} from 'lucide-react';import {useAuth} from '../../context/AuthContext';import './Signup.css';
export default function Signup(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[show,setShow]=useState(false),[role,setRole]=useState('citizen'),[error,setError]=useState(''),[loading,setLoading]=useState(false);const {register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();if(password!==confirm){setError('Passwords do not match.');return}setError('');setLoading(true);try{const user=await register({name,email,password,role});nav(`/${user.role}/dashboard`)}catch(err){setError(err.response?.data?.error||'Unable to create account.')}finally{setLoading(false)}};return <div className="auth-page"><Link className="back-home" to="/"><ArrowLeft size={15}/> Back to RIVORA</Link><div className="auth-visual"><div className="auth-brand"><ShieldCheck size={18}/> RIVORA</div><div className="auth-copy"><div className="eyebrow">Citizen + Authority network</div><h1>Build a safer<br/><span>urban response.</span></h1><p>Create your RIVORA workspace. Admin registration is restricted to internal provisioning.</p></div></div><div className="auth-form-wrap"><form className="auth-form glass" onSubmit={submit}><div className="eyebrow">JOIN RIVORA</div><h2>Create Account</h2><p className="muted">Your identity will be stored in PostgreSQL through the API.</p><label>Full Name<input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name" required/></label><label>Email<input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" required/></label><label>Password<div className="password-wrap"><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} minLength="6" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label><label>Confirm Password<input className="input" value={confirm} onChange={e=>setConfirm(e.target.value)} type={show?'text':'password'} minLength="6" required/></label><label>Role<select className="select" value={role} onChange={e=>setRole(e.target.value)}><option value="citizen">Citizen</option><option value="authority">Authority</option></select></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary auth-submit" disabled={loading}>{loading?'Creating…':'Sign Up'}</button><p className="auth-foot">Already have an account? <Link to="/login">Login</Link></p></form></div></div>}
