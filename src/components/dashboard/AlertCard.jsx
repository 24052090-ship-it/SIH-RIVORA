import {AlertTriangle,Clock,MapPin} from 'lucide-react';import Badge from '../common/Badge/Badge';import './AlertCard.css';export default function AlertCard({alert}){const tone=alert.level.toLowerCase();return <div className="alert-card"><div className="alert-icon"><AlertTriangle size={16}/></div><div className="alert-body"><div className="alert-row"><Badge tone={tone==='warning'?'medium':tone==='critical'||tone==='high'?'critical':'info'}>{alert.level}</Badge><span><Clock size={12}/> {alert.time}</span></div><strong>{alert.location}</strong><p>{alert.message}</p><div className="alert-loc"><MapPin size={12}/> Bengaluru Urban</div></div></div>}