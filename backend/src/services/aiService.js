import {env} from '../config/env.js';async function post(path,payload){const r=await fetch(`${env.aiServiceUrl}${path}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});if(!r.ok){const t=await r.text();throw new Error(`AI service ${r.status}: ${t}`)}return r.json()}export const predictFlood=f=>post('/predict/flood',f);export const predictFusion=f=>post('/predict/fusion',f);
