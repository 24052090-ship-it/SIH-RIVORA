import {Router} from 'express';import {requireAuth} from '../middleware/auth.js';import {status,weatherNowcast,googleRoute,sentinelScenes,geocode,reverseGeocode} from '../controllers/providersController.js';const r=Router();r.get('/status',requireAuth,status);r.get('/weather/nowcast',requireAuth,weatherNowcast);r.post('/google/routes',requireAuth,googleRoute);r.get('/satellite/scenes',requireAuth,sentinelScenes);r.get('/geocode',requireAuth,geocode);r.get('/reverse-geocode',requireAuth,reverseGeocode);export default r;
