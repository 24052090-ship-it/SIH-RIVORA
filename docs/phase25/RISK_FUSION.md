# Phase 25 Risk Fusion

Recommended flow:

AI predictions + sensor observations + GIS context
→ normalized signals
→ transparent weighted fusion
→ risk score
→ risk level
→ contributing factors
→ incident/alert decision

Do not treat a weighted score as a validated probability unless it has been statistically calibrated against labeled local outcomes.

Keep model provenance for every component and log the final fusion decision.
